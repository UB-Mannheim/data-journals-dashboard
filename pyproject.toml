[project]
name = "data-journals-dashboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.3.3",
    "pyyaml>=6.0",
    "requests>=2.33.1",
]

[project.scripts]
dj = "data_processing:main"
